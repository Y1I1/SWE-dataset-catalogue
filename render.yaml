services:
  - type: web
    name: dataset-catalogue
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: flask --app run.py init-db && gunicorn run:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
      - key: FLASK_CONFIG
        value: prod
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
